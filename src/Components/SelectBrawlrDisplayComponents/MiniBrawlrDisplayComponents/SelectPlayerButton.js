import React, {Component} from 'react';

export default class SelectPlayerButton extends Component {
    
    render() {
        return(
            <button className="selectBrawlrButton">Select PX</button>
        )
    }
}