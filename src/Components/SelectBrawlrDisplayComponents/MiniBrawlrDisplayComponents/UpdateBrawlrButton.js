import React, {Component} from 'react';

export default class UpdateBrawlrButton extends Component {
    
    render() {
        return(
            <button className="updateBrawlrButton">Update</button>
        )
    }
}