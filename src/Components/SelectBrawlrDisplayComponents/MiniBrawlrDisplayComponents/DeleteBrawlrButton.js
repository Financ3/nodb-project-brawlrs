import React, {Component} from 'react';

export default class DeleteBrawlrButton extends Component {
    
    render() {
        return(
            <button className="deleteBrawlrButton">Delete</button>
        )
    }
}