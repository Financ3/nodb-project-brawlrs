import React, {Component} from 'react';

export default class AddBrawlerButton extends Component {
    
    render() {
        return(
            <button className="addBrawlerButton">Add New Brawler</button>
        )
    }
}